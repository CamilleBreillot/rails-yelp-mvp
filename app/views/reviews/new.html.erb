<h1>Yelp</h1>

<h2>Leave a review to <%= @restaurant.name %></h2>

<%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "Leave a review", class:"btn btn-danger" %>
  <% end %>
